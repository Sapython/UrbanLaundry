import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.shreeva.urbanlaundry',
  appName: 'UrbanLaundry',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
